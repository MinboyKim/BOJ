#include <bits/stdc++.h>

using namespace std;

int main() { double a, b;
    cin >> a >> b;
    double re = 0;
    re = a * 2 + (b* 2*3.141592);
    cout << fixed;
    cout.precision(6);
    cout << re;
}   