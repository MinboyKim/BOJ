#include <bits/stdc++.h>

using namespace std;

pair<int, int> pii[50505];
int main() {
    int curt = 0;

    int n,a,b;
    cin >> n;
    for (int i = 0; i < n; i++){
        cin>>pii[i].first >> pii[i].second;
    }
    for (int i = 0; i < n; i++){
        
    }
}